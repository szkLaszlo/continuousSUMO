from setuptools import setup

setup(name='continuousSUMO',
      version='0.0.1',
      install_requires=['gym', 'numpy', 'torch', 'matplotlib', 'easygui', 'tensorboard']
      # And any other dependencies required
      )
